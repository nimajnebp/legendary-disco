##
# ---------------------------------
# Layout-builder layout definitions
# ---------------------------------
##
one_column_fluid:
  label: 'One column - fluid'
  category: 'Layouts'
  template: templates/layouts/one-column-fluid
  default_region: main
  icon_map:
    - [main]
  regions:
    main:
      label: Main content (100%) (fluid)

one_column_contained:
  label: 'One column - contained'
  category: 'Layouts'
  template: templates/layouts/one-column-contained
  default_region: main
  icon_map:
    - [main]
  regions:
    main:
      label: Main content (100%) (contained)
